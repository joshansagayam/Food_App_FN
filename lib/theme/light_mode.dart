import 'package:flutter/material.dart';

ThemeData lightmood = ThemeData(
  colorScheme: const ColorScheme.light(
    surface: Color(0xFFFFFFFF), // White background
    primary: Color(0xFF6200EE), // Primary color for light theme (Purple 500)
    secondary:
        Color(0xFF03A9F4), // Secondary color for light theme (Light Blue 500)
    tertiary: Color(0xFF03DAC6), // Tertiary color for light theme (Teal 200)
    inversePrimary: Color(0xFF3700B3), // Inverse primary color (Purple 700)
  ),
);
